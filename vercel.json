{
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p dist/src && cp -r src/assets dist/src/",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
